Feature:"Ao executar metodos utilitarios para UsuarioController via UserService"

    Scenario: 1 - Listar todos usuarios
                  When: utilizar metodo listarTodos
                  Then: deve retornar lista paginada de usuarios

    Scenario: 2 - Procurar user por ID
                  Given: id valido para user cadastrado
                  When: procurar user por id
                  Then: deve retornar user

                  Given: id invalido
                  When: procurar user por id
                  Then: deve lançar BadRequestException

    Scenario: 3 - Procurar user por Username
                  Given: username valido para user cadastrado valido
                  When: procurar user por Username
                  Then: deve retornar user

                  Given: username invalido
                  WHen: procurar por user por username
                  Then: deve lançar BadRequestException

    Scenario: 4 - Salvar user
                  Given: user valido para salvar
                  When: salvar user
                  THen: deve retornar user salvo

    Scenario: 4 - Deletar user
                  Given: id valido para user salvo no banco
                  WHen: deletar user por id
                  Then: deve remover user com sucesso

                  Given: id invalido
                  When: deletar user por id
                  Then: deve lançar BadRequestException